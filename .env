# Public build configuration. Every VITE_* variable is embedded in the client bundle — never put secrets here.

# Absolute URL where the site is published, without trailing slash.
# Used for canonical, Open Graph, structured data, robots.txt and sitemap.xml.
VITE_SITE_URL=https://juniorborlesti.ro
